local options = {
  ensure_installed = { 
    "lua", 
    "c",
    "python",
    "java",
    "rust"
  },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = false },
}

return options
